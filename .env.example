# Copy to .env and fill in. Never commit .env — it holds real credentials.

NODE_ENV=development
PORT=4000

# Comma-separated list of origins allowed to call this API.
CORS_ORIGINS=http://localhost:5173

# MongoDB. Local for dev; an Atlas SRV string for staging/production.
MONGODB_URI=mongodb://127.0.0.1:27017/sugarloop

# Signing secrets. Generate with: openssl rand -base64 48
# Customer and staff tokens are signed with DIFFERENT secrets, so a leaked
# customer token can never be replayed against a staff endpoint.
JWT_CUSTOMER_SECRET=replace-me-customer
JWT_STAFF_SECRET=replace-me-staff

# Customer sessions last 4 days (the agreed OTP re-verification window).
JWT_CUSTOMER_EXPIRES_IN=4d
JWT_STAFF_ACCESS_EXPIRES_IN=15m
JWT_STAFF_REFRESH_EXPIRES_IN=7d

LOG_LEVEL=debug

# --- Customer verification --------------------------------------------------
# Checkout verifies the EMAIL ADDRESS, not the phone number. The number is still
# collected and is still what the branch rings — it is just no longer proven, which
# is a real weakening of the prank-order defence and a deliberate trade while the
# WhatsApp sender sits in Meta's review. See services/otpDelivery.service.js.
#
# How OTP codes reach a customer.
#   log        prints the code to this server's console and sends NOTHING. Works with
#              no third-party account, and echoes the code to the browser so checkout
#              is testable offline. REFUSED in production.
#   email      mails it, via the EMAIL_TRANSPORT / SMTP_* settings below. This is what
#              production runs. Refused in production unless EMAIL_TRANSPORT=smtp.
#   whatsapp   delivers to a PHONE. Needs a Meta Business account + an approved
#              `sugarloop_otp` template. Kept for the day phone verification returns —
#              the server refuses to boot with it while checkout sends an address.
#   sms        delivers to a PHONE. Needs a Twilio account. Never implemented.
OTP_TRANSPORT=log

# --- Order notifications ----------------------------------------------------
# How order updates reach customers and branches. Six Utility-category templates:
# sugarloop_order_placed, _out_for_delivery, _ready_for_pickup, _order_completed,
# _new_order_staff, _enquiry_staff.
#
# Separate from OTP_TRANSPORT on purpose. Meta approves each template on its own and
# rejections are common, so `sugarloop_otp` (Authentication category) can clear review
# while an order template is still on its second submission — one switch would mean
# holding back a working OTP flow until the slowest template lands.
#
#   log        renders the message to this console and sends NOTHING. REFUSED in
#              production, where it means customers never hear their order was placed
#              and — worse — the branch never hears an order came in.
#   whatsapp   needs the Meta Business account and all six templates approved
NOTIFY_TRANSPORT=log

# --- WhatsApp Cloud API credentials -----------------------------------------
# Shared by both transports above. Only needed when one of them is set to `whatsapp`;
# the server refuses to start in that case if the token or the number id is missing.
#
# The sending number is the business number registered with Meta. It is a SENDER only:
# a number on the Cloud API cannot receive its own messages and can no longer be used
# in the normal WhatsApp app, so never put it in ENQUIRY_NOTIFY_PHONE, in
# ADMIN_ESCALATION_PHONE, or on a branch record.
#
# WHATSAPP_PHONE_NUMBER_ID is the numeric id beside the number in WhatsApp Manager
# (Account tools > Phone numbers), roughly 15 digits — NOT the phone number itself.
# The number is the thing on that screen that looks like an identifier, which is why
# this is the field people fill in wrong; a phone number here fails every send with a
# 404 on a URL that looks entirely reasonable in the logs.
# WHATSAPP_PHONE_NUMBER_ID=

# Generate under Business settings > Users > System users, with expiry set to Never and
# the whatsapp_business_messaging + whatsapp_business_management scopes. The token in
# the developer console's Getting Started panel is NOT this: it expires after 24 hours
# and will take the shop down overnight.
# WHATSAPP_TOKEN=

# The locale every template was approved under, exactly as submitted. A template
# approved as `en_US` and requested as `en` does not exist as far as the API is
# concerned, and says so only as error code 132001.
# WHATSAPP_TEMPLATE_LANG=en

# Whether `sugarloop_otp` was built with a copy-code button — Meta's default for an
# Authentication template. The payload has to agree with the template: declare the
# button and omit it and the send is rejected; send one the template does not declare
# and that is rejected too.
# WHATSAPP_OTP_HAS_COPY_BUTTON=true

# Pinned so a Meta release cannot change behaviour on its own.
# WHATSAPP_API_VERSION=v21.0

# Where the WhatsApp copy of a corporate enquiry goes. Left empty, that message is
# skipped and logged; the notification EMAIL is unaffected either way.
# ENQUIRY_NOTIFY_PHONE=+923001234567

# --- Redis (rate limits + the order escalation queue) -----------------------
# OPTIONAL. Without it the API runs exactly as before, with two caveats:
#
#   1. Rate limits become per-process and reset on every restart. On OTP that matters —
#      the per-IP cap IS the control stopping a script walking six-digit codes.
#   2. Unacknowledged orders are NOT chased. An order nobody opens sits in `placed`
#      until a customer rings to ask where it is.
#
# Both are fine for one developer on a laptop and wrong for a real shop, so both warn
# loudly at boot outside development.
# REDIS_URL=redis://127.0.0.1:6379

# How long an order may sit unacknowledged before each rung is chased. Client decision.
ORDER_ESCALATION_MANAGER_MINUTES=5
ORDER_ESCALATION_ADMIN_MINUTES=10

# The third rung, and the one that ends it: how long an order may sit in `placed` before
# the system fails it and emails the customer an apology. 0 switches it off, which
# restores the old behaviour — an order waiting forever while nobody is told anything.
#
# Unlike the two chases above this needs no Redis: it is a sweep over the orders table,
# so it still works on a box where REDIS_URL is unset. See services/orderExpiry.service.js.
ORDER_AUTO_CANCEL_MINUTES=30

# How often that sweep runs. This decides when a deadline is noticed, not what it is.
ORDER_EXPIRY_SWEEP_SECONDS=60

# Who the second rung reaches. A StaffUser has no phone field, so this cannot be looked
# up. Falls back to ENQUIRY_NOTIFY_PHONE; empty means the admin chase is logged and
# skipped rather than sent to a guessed number.
# ADMIN_ESCALATION_PHONE=+923001234567

# --- Geocoding --------------------------------------------------------------
# Turns a typed delivery address into coordinates, to pick the nearest branch.
#   nominatim  OpenStreetMap. Free, no key, works today. Rate-limited to ~1 req/s and
#              weak on specific Pakistani buildings — it knows "DHA Phase 2" but not
#              "Nadir Arcade". Fine for development, a stand-in for production.
#   google     the intended provider. Needs GOOGLE_MAPS_API_KEY below.
GEOCODER=nominatim

# Google Cloud > APIs & Services > Credentials, with the Geocoding API enabled and
# billing switched on. 10,000 lookups/month are free, then $5 per 1,000 — and results
# are cached here for 90 days, so repeat addresses cost nothing.
#
# RESTRICT THE KEY to the Geocoding API, and to your server's IP. An unrestricted key
# that leaks is billed to the client until somebody notices.
# The server refuses to start with GEOCODER=google and no key.
# GOOGLE_MAPS_API_KEY=

# --- Delivery distance ------------------------------------------------------
# How far a delivery is, and therefore whether it is in range at all.
#   straightline  great-circle distance, compared against a branch's deliveryRadiusKm.
#                 Needs nothing, and understates real journeys badly: a Westridge
#                 address 3.65 km from NUST H-12 in a straight line is a 9.97 km ride,
#                 because Nur Khan airbase forces the road around it. The detour factor
#                 across this city runs from 1.2x to 2.7x, so one radius cannot mean the
#                 same thing everywhere.
#   osrm          real road distance AND ride time from a self-hosted OSRM. The rule
#                 becomes maxDeliveryRoadKm — 5 km of actual riding, per branch. Ride
#                 time is measured and shown too, but does not decide anything.
ROUTER=straightline

# Base URL of your OSRM server. The server refuses to start with ROUTER=osrm and no URL.
#
# Run it next to the API (one-time, ~10 min of downloads):
#   curl -LO https://download.geofabrik.de/asia/pakistan-latest.osm.pbf
#   docker run -t -v "${PWD}:/data" osrm/osrm-backend osrm-extract -p /opt/bike.lua /data/pakistan-latest.osm.pbf
#   docker run -t -v "${PWD}:/data" osrm/osrm-backend osrm-partition /data/pakistan-latest.osrm
#   docker run -t -v "${PWD}:/data" osrm/osrm-backend osrm-customize /data/pakistan-latest.osrm
#   docker run -d -p 5000:5000 -v "${PWD}:/data" --restart unless-stopped \
#     --name sugarloop-osrm osrm/osrm-backend osrm-routed --algorithm mld /data/pakistan-latest.osrm
#
# bike.lua, not car.lua: riders are on motorbikes, and the bike profile follows the
# lanes and cut-throughs a car profile routes around. Swap to car.lua if that changes.
#
# NOT router.project-osrm.org — that is OSRM's demo host, explicitly not for production
# use. It is fine for a local experiment and nothing else.
# OSRM_URL=http://127.0.0.1:5000

# --- Email ------------------------------------------------------------------
# How outbound email leaves the server. Today that means corporate gifting enquiries.
#   log    prints the whole message to this console and sends NOTHING. REFUSED in
#          production, where it would silently drop every lead.
#   smtp   real delivery. Needs the four SMTP_* values below.
#
# Unlike WhatsApp, this one is not blocked on anything: the shop already has
# sugarlooppk@gmail.com, and Gmail issues an app password in about two minutes
# (Google Account > Security > 2-Step Verification > App passwords). Use that app
# password below, NOT the account's real password.
EMAIL_TRANSPORT=log

# Gmail: smtp.gmail.com / 587. The server refuses to start with EMAIL_TRANSPORT=smtp
# and any of host, user or password missing.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=sugarlooppk@gmail.com
# SMTP_PASSWORD=

# The From: address. Must be one the SMTP account is allowed to send as.
# EMAIL_FROM=Sugarloop <sugarlooppk@gmail.com>

# Where corporate gifting enquiries are emailed.
# ENQUIRY_NOTIFY_EMAIL=sugarlooppk@gmail.com

# --- Product images (S3) ----------------------------------------------------
# Where product photos live. Without a bucket the catalogue still works and simply
# serves no images, which is the current state.
#
# Credentials are deliberately NOT here. The AWS SDK finds them itself: environment,
# the shared config file, or — on EC2 — the instance role, which involves no long-lived
# secret at all and is what production should use. Attach a role with s3:PutObject,
# s3:GetObject and s3:DeleteObject on this bucket and set nothing else.
#
# s3:GetObject is needed even though nothing here reads image bytes: confirming an upload
# does a HeadObject to check the file actually arrived and is the size and type it
# claimed, and HeadObject is authorised as GetObject.
# S3_BUCKET=sugarloop-media
# S3_REGION=ap-south-1
#
# ⚠️ The bucket needs a CORS rule, or every upload from the admin console fails.
# The browser PUTs the file straight to S3 — see services/imageStorage.service.js for
# why it does not come through this API — and that is a cross-origin request. Without
# this the browser blocks it before S3 ever sees it, and the console shows a CORS error
# that looks like a bug in this server rather than a missing bucket setting.
#
#   [
#     {
#       "AllowedOrigins": ["https://admin.sugarloop.pk"],
#       "AllowedMethods": ["PUT"],
#       "AllowedHeaders": ["content-type", "cache-control"],
#       "ExposeHeaders": [],
#       "MaxAgeSeconds": 3000
#     }
#   ]
#
# AllowedHeaders must list exactly the headers the signed URL requires the browser to
# send; the upload response names them in `headers`. AllowedOrigins takes the admin
# console's origin — the same one in CORS_ORIGINS above — never "*", which would let any
# site spend a signed URL it managed to obtain.
#
# Worth adding a lifecycle rule too: an admin who requests an upload URL and then closes
# the tab leaves an object no product references, and nothing ever collects it. A rule
# expiring objects under `products/` that are still untagged after a day costs nothing
# and stops the bucket accumulating them forever.

# Public base URL the images are served from — the CloudFront domain, no trailing
# slash. Without it the bucket's own endpoint is used, which works but is uncached and
# needs the objects to be publicly readable.
# ASSET_BASE_URL=https://dxxxxxxxxxxxxx.cloudfront.net

# Used by `npm run seed` only — the API never reads it. Required to seed staff
# outside development; in development the script generates one and prints it once.
# SEED_ADMIN_PASSWORD=
